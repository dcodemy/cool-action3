name: 'Cool Action'
description: 'A GitHub Action that prints coool'
runs:
  using: 'composite'
  steps:
    - run: echo "hacked again"
      shell: bash
    - name: test
      uses: dcodemy/cool-action3@v1
